import type { CollectionConfig } from "payload";

import { fieldOperatorOrAdmin } from "@/payload/access";

export const DeliveryMessages: CollectionConfig = {
  slug: "delivery-messages",
  admin: {
    useAsTitle: "idempotencyKey",
    group: "Video Teslimat",
    defaultColumns: ["group", "recipientPhone", "status", "scheduledAt", "sentAt"],
  },
  access: {
    read: fieldOperatorOrAdmin,
    create: () => false,
    update: () => false,
    delete: () => false,
  },
  timestamps: true,
  fields: [
    { name: "group", type: "relationship", relationTo: "operation-groups", required: true, index: true },
    { name: "video", type: "relationship", relationTo: "operation-videos", required: true, index: true },
    { name: "donation", type: "relationship", relationTo: "donations", index: true },
    { name: "participant", type: "relationship", relationTo: "donation-participants", index: true },
    { name: "member", type: "relationship", relationTo: "operation-group-members", index: true },
    {
      name: "messageType",
      type: "select",
      required: true,
      defaultValue: "normal",
      options: ["normal", "correction", "code_renewal", "test"],
      admin: { readOnly: true },
      access: { update: () => false },
    },
    {
      name: "recipientRole",
      type: "select",
      options: ["payer", "participant", "test"],
      admin: { readOnly: true },
      access: { update: () => false },
    },
    {
      name: "recipientPhone",
      type: "text",
      access: {
        read: ({ req }) => req.user?.role === "admin",
        update: ({ req }) => req.user?.role === "admin",
      },
    },
    {
      name: "normalizedPhone",
      type: "text",
      access: {
        read: ({ req }) => req.user?.role === "admin",
        update: ({ req }) => req.user?.role === "admin",
      },
    },
    {
      name: "recipientPhoneHash",
      type: "text",
      required: true,
      index: true,
      admin: { hidden: true },
      access: { read: () => false, update: () => false },
    },
    { name: "body", type: "textarea" },
    {
      name: "bodySnapshot",
      type: "textarea",
      admin: { readOnly: true },
      access: { update: () => false },
    },
    {
      name: "messageSnapshot",
      type: "json",
      admin: { readOnly: true },
      access: { update: () => false },
    },
    {
      name: "systemPayloadSnapshot",
      type: "json",
      admin: { hidden: true },
      access: { read: () => false, update: () => false },
    },
    {
      name: "idempotencyKey",
      type: "text",
      required: true,
      unique: true,
      index: true,
      admin: { hidden: true },
      access: { read: () => false, update: () => false },
    },
    {
      name: "status",
      type: "select",
      required: true,
      defaultValue: "draft",
      index: true,
      options: [
        "draft",
        "countdown",
        "queued",
        "paused",
        "sending",
        "sent",
        "delivered",
        "read",
        "failed",
        "cancelled",
        "manual_sent",
      ],
    },
    { name: "dispatchBatchId", type: "text", index: true },
    { name: "scheduledAt", type: "date", index: true },
    { name: "lockedAt", type: "date", index: true },
    { name: "lockedBy", type: "text" },
    {
      name: "providerMessageId",
      type: "text",
      index: true,
      admin: { readOnly: true },
      access: { update: () => false },
    },
    { name: "providerStatus", type: "text", index: true, admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "providerPayload",
      type: "json",
      admin: { readOnly: true },
      access: {
        read: ({ req }) => req.user?.role === "admin",
        update: () => false,
      },
    },
    { name: "attemptCount", type: "number", required: true, min: 0, defaultValue: 0 },
    { name: "lastError", type: "textarea" },
    { name: "lastErrorCode", type: "text", admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "retryClass",
      type: "select",
      required: true,
      defaultValue: "none",
      options: ["none", "transient", "permanent", "ambiguous"],
      admin: { readOnly: true },
      access: { update: () => false },
    },
    { name: "nextRetryAt", type: "date", index: true, admin: { readOnly: true }, access: { update: () => false } },
    { name: "lastAttemptAt", type: "date", admin: { readOnly: true }, access: { update: () => false } },
    { name: "providerLookupAt", type: "date", admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "accessTokenDigest",
      type: "text",
      required: true,
      unique: true,
      index: true,
      admin: { hidden: true },
      access: { read: () => false, update: () => false },
    },
    { name: "expiresAt", type: "date", required: true, index: true },
    { name: "sentAt", type: "date" },
    { name: "deliveredAt", type: "date" },
    { name: "readAt", type: "date" },
    { name: "failedAt", type: "date" },
    { name: "manualSentAt", type: "date", admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "manualSentBy",
      type: "relationship",
      relationTo: "users",
      admin: { readOnly: true },
      access: { update: () => false },
    },
    { name: "manualSentReason", type: "textarea", admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "isTest",
      type: "checkbox",
      required: true,
      defaultValue: false,
      admin: { readOnly: true },
      access: { update: () => false },
    },
    { name: "testNumberKey", type: "text", admin: { readOnly: true }, access: { update: () => false } },
    {
      name: "testFingerprint",
      type: "text",
      admin: { hidden: true },
      access: { read: () => false, update: () => false },
    },
    { name: "redactedAt", type: "date", index: true },
  ],
};
